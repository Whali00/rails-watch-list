<h1>New List</h1>

<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.submit class: "btn btn-primary" %>
<% end %>

<%= link_to "Back to the lists", lists_path(@lists), class: 'btn btn-light' %>
